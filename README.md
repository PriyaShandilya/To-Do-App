This To-Do list App is built using HTML, CSS, EJS template, js . 
Node.js, Express Server right now i have not connected this to database used array objects to store data.
To Run this project clone this repo on your machine and install following, if you have already installed node otherwise install then proceed for the below installation :
npm i express
npm i nodemon
npm i ejs
