This To-Do list App is built using HTML, CSS, EJS template, js . 
Node.js, Express Server right now i have not connected this to databse used array objects to store data.
To Run this on your machine clone this repo on your machine and install following, if you have already installed node other install then proceed for the below installation :
npm i express
npm i nodemon
npm i ejs
